document.querySelectorAll('.btn').forEach(button => {
    button.addEventListener('click', () => {
        alert('This is a placeholder for functionality such as linking to the backend.');
    });
});
